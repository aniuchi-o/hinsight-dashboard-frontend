VITE_APP_VERSION=1.0.0
VITE_APP_ENV=production
# VITE_API_BASE_URL is injected at CI/CD build time — do not hardcode here
# VITE_API_KEY is injected at CI/CD build time — add as a GitHub Actions Secret
